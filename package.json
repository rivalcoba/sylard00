{
  "name": "00-ehboilerplate",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "npm-run-all clean transpile",
    "clean": "rimraf dist-server",
    "dev": "NODE_ENV=development babel-node ./server/bin/www",
    "dev:watch": "NODE_ENV=development nodemon --exec babel-node ./server/bin/www",
    "prod": "NODE_ENV=production npm run build && node ./dist-server/bin/www",
    "start": "npm run prod",
    "transpile": "babel ./server --out-dir dist-server --copy-files"
  },
  "dependencies": {
    "babel-plugin-module-resolver": "^4.0.0",
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "dotenv": "^8.2.0",
    "express": "~4.16.1",
    "hbs": "~4.0.4",
    "http-errors": "~1.6.3",
    "morgan": "~1.9.1",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2"
  },
  "devDependencies": {
    "@babel/cli": "^7.10.5",
    "@babel/core": "^7.10.5",
    "@babel/node": "^7.10.5",
    "@babel/preset-env": "^7.10.4",
    "nodemon": "^2.0.4"
  }
}
